# Vite mode: staging (npm run build:staging) for the deployed test environment.
# The actual staging API URL is injected at build time via ARG VITE_API_URL in the
# frontend Dockerfile; this relative default is used only when no arg is provided.
VITE_API_URL=/api/v1
