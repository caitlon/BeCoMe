# Vite mode: production (npm run build). Served by nginx on the same origin as
# the API, so a relative path works. On deploy, ARG VITE_API_URL in the frontend
# Dockerfile overrides this when the API lives on a different host.
VITE_API_URL=/api/v1
