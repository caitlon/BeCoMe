# Docker Compose Environment Variables
# Copy this file to .env in the docker/ directory before running docker compose
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up --build

# ============================================
# REQUIRED - Must be set before running
# ============================================

# Secret key for JWT tokens - GENERATE YOUR OWN!
# Run: openssl rand -hex 32
SECRET_KEY=<paste-your-generated-key-here>

# ============================================
# OPTIONAL - Defaults shown below
# ============================================

# Database credentials (change for production!)
# POSTGRES_USER=become
# POSTGRES_PASSWORD=become
# POSTGRES_DB=become

# API settings
# DEBUG=false
# UVICORN_WORKERS=4

# CORS allowed origins (JSON array)
# CORS_ORIGINS=["http://localhost:3000"]
