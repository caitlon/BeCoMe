name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-24.04
    
    steps:
      - name: Debug runner info
        run: |
          echo "Runner OS: $RUNNER_OS"
          echo "Runner architecture: $(uname -m)"
          echo "Available disk space:"
          df -h
          echo "Memory info:"
          free -h
      
      - uses: actions/checkout@v5
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.7"
      
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
      
      - name: Cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-
      
      - name: Install dependencies
        run: |
          uv sync --dev --python 3.13.7
          ls -la .venv/bin/ || echo "No .venv/bin found"
      
      - name: Run ruff
        run: .venv/bin/ruff check .
      
      - name: Run mypy
        run: .venv/bin/mypy src/

  tests:
    runs-on: ubuntu-24.04
    needs: lint
    
    steps:
      - uses: actions/checkout@v5
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13.7"
      
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
      
      - name: Cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-
      
      - name: Install dependencies
        run: |
          uv sync --dev --python 3.13.7
          ls -la .venv/bin/ || echo "No .venv/bin found"
      
      - name: Debug Python environment
        run: |
          .venv/bin/python --version
          uv --version
          .venv/bin/pip list
      
      - name: Run tests
        run: .venv/bin/pytest

